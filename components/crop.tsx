export default function Crop({src,box,alt,className=""}:{src:string;box:number[];alt:string;className?:string}){const[fw,fh,x,y,w,h]=box;return <div className={`art-crop ${className}`} style={{aspectRatio:`${w}/${h}`,"--crop-ratio":w/h} as React.CSSProperties}><img src={src} alt={alt} draggable={false} style={{position:"absolute",maxWidth:"none",width:`${fw/w*100}%`,height:"auto",left:`${-x/w*100}%`,top:`${-y/h*100}%`}}/></div>}